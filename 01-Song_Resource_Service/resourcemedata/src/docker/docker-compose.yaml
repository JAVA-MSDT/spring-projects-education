services:
  db:
    container_name: pg_resource_metadata
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: "resource_metadata"
      POSTGRES_USER: "resource_metadata"
      POSTGRES_PASSWORD: "resource_metadata"
    volumes:
      - C:\Docker\Postgres\resource_metadata:/var/lib/postgresql/data
    ports:
      - 5434:5432
  pgadmin:
    container_name: pgAdmin
    image: dpage/pgadmin4
    restart: always
    depends_on:
      - db
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@admin.com
      PGADMIN_DEFAULT_PASSWORD: admin
    volumes:
      - C:\Docker\Postgres\pgadmin:/var/lib/pgadmin
    ports:
      - 5430:80